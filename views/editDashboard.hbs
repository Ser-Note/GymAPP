<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit Workouts</title>
    <script src="./js/authHelper.js" defer></script>
    <script src="./js/frontEditWorkout.js" defer></script>
</head>
<body>
    
    <div id="winterSnoweContainer"></div>

    <main class="dashboard">
        <h1>{{title}}</h1>
        {{#if user.isAuthenticated}}
            {{#each exercises}}
                <div class="edit-workout-form">
                    <form id="workoutEditForm" method="POST" action="/editWorkouts?_method=PUT">
                        <label for="workout_name">Workout Name:</label>
                        <input type="text" id="workout_name" name="workout_name" value="{{this.workoutname}}" required>

                        <label for="exercise_name">Exercise Name:</label>
                        <input type="text" id="exercise_name" name="exercise_name" value="{{this.name}}" required>

                        <label for="sets">Sets:</label>
                        <input type="number" id="sets" name="sets" value="{{this.targetSets}}" required>

                        <label for="reps">Reps:</label>
                        <input type="number" id="reps" name="reps" value="{{this.targetReps}}" required>

                        <button type="submit">Save Changes</button>
                    </form>
                </div>
            {{/each}}
        {{else}}
               <div class="notAuthenticated-form">
                    <p>You must be authetnicated to edit workouts.</p>
               </div>
        {{/if}}
    </main>





</body>
</html>